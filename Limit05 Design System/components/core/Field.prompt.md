One-line: the only form control the brand defines — label above, white input, Sage Mist border.

```jsx
<Field label="Email nhận ưu đãi" placeholder="ban@vidu.vn" />
```

- Input padding `12px 14px`, radius `8px`; label is Muted Olive at `.88rem`.
- Focus: `outline: 2px solid #D66847` and the border turns Terracotta Rust. This is intentionally the same accent as the button focus ring.
